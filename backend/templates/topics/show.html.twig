{% extends 'base.html.twig' %}

{% block title %}Topics{% endblock %}

{% block body %}
    <h1>Topics</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ topic.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ topic.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_topics_index') }}">back to list</a>

    <a href="{{ path('app_topics_edit', {'id': topic.id}) }}">edit</a>

    {{ include('topics/_delete_form.html.twig') }}
{% endblock %}
